{
  "name": "forgewright",
  "private": true,
  "type": "module",
  "workspaces": ["packages/*"],
  "catalog": {
    "typescript": "^5.7.0",
    "zod": "^3.24.0",
    "ai": "^4.0.0",
    "@ai-sdk/anthropic": "^1.0.0",
    "@ai-sdk/openai": "^1.0.0",
    "@ai-sdk/google": "^1.0.0"
  },
  "scripts": {
    "build": "bun run --filter '*' build",
    "test": "bun test",
    "typecheck": "tsc --noEmit",
    "forgewright": "bun run packages/cli/src/index.ts"
  },
  "devDependencies": {
    "typescript": "catalog:",
    "@types/bun": "latest"
  }
}
